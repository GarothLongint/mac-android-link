name: MacLink
options:
  bundleIdPrefix: com.maclink
  deploymentTarget:
    macOS: "13.0"
  xcodeVersion: "15"

packages:
  SwiftProtobuf:
    url: https://github.com/apple/swift-protobuf.git
    from: 1.25.0

targets:
  MacLink:
    type: application
    platform: macOS
    sources:
      - path: MacLink/Sources
      - path: MacLink/Assets.xcassets
    info:
      path: MacLink/Sources/App/Info.plist
      properties:
        CFBundleIdentifier: com.maclink.app
        CFBundleName: MacLink
        CFBundleDisplayName: MacLink
        CFBundleVersion: "1"
        CFBundleShortVersionString: "0.1.0"
        LSUIElement: true                        # Menu bar only (no Dock icon)
        NSUserNotificationAlertStyle: alert
        NSUserNotificationsUsageDescription: "MacLink wyświetla powiadomienia z Twojego telefonu Android."
        NSMicrophoneUsageDescription: "MacLink używa mikrofonu do prowadzenia rozmów telefonicznych przez Maca."
    settings:
      base:
        SWIFT_VERSION: "5.9"
        MACOSX_DEPLOYMENT_TARGET: "13.0"
        PRODUCT_BUNDLE_IDENTIFIER: com.maclink.app
        ENABLE_HARDENED_RUNTIME: YES
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        CODE_SIGN_ENTITLEMENTS: MacLink/MacLink.entitlements
    dependencies:
      - package: SwiftProtobuf
        product: SwiftProtobuf
